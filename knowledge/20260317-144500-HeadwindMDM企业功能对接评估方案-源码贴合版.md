# Headwind MDM Enterprise 功能对接评估方案（源码贴合版）

> 本文档基于对 hmdm-server (后端) 和 hmdm-android (客户端) 实际源码的深度分析，提供具体的集成点和代码映射。
> 分析时间：2026-03-17
> 后端版本：基于 notification/src/main/java/com/hmdm/notification/PushService.java 等核心文件
> 客户端版本：基于 app/src/main/java/com/hmdm/launcher/json/PushMessage.java 等核心文件

## 1. 架构分析

### 1.1 后端架构（hmdm-server）

```
hmdm-server/
├── common/src/main/java/com/hmdm/
│   ├── persistence/domain/         # 数据库实体类
│   │   ├── Device.java             # 设备实体（含mdmMode, kioskMode字段）
│   │   ├── Configuration.java      # 配置实体（含kiosk相关配置）
│   │   ├── WhiteLabelBuild.java    # 白标构建配置
│   │   └── RebrandingSettings.java # 重命名设置
│   ├── persistence/mapper/         # MyBatis Mapper接口
│   └── rest/resource/              # REST API控制器
│       └── DeviceResource.java     # 设备管理API
├── notification/src/main/java/com/hmdm/notification/
│   ├── PushService.java            # Push消息服务（已包含远程命令方法）
│   ├── persistence/domain/PushMessage.java  # Push消息类型定义
│   └── ...
├── plugins/                        # 插件架构（关键！）
│   ├── audit/                      # 审计插件
│   ├── deviceinfo/                 # 设备信息插件
│   ├── messaging/                  # 消息插件
│   ├── push/                       # Push消息管理插件
│   └── ...
└── server/src/main/java/com/hmdm/rest/resource/
    └── PublicResource.java         # 设备注册/同步API
    └── SyncResource.java           # 设备配置同步（含rebranding数据）
```

### 1.2 客户端架构（hmdm-android）

```
app/src/main/java/com/hmdm/launcher/
├── json/                           # JSON实体类
│   ├── PushMessage.java            # Push消息类型（需新增常量）
│   └── ServerConfig.java           # 服务器配置（已含appName, vendor）
├── pro/
│   └── ProUtils.java               # Pro/Enterprise功能存根
├── server/
│   └── ServerService.java          # REST API接口定义
├── service/
│   └── LocationService.java        # 位置服务（已存在）
├── util/
│   ├── PushNotificationMqttWrapper.java  # MQTT连接
│   └── CryptoHelper.java           # 加密工具
└── worker/
    └── PushNotificationProcessor.java  # Push消息处理器
```

---

## 2. 后端源码集成点（已存在功能）

### 2.1 Push消息类型定义（已存在！）

**文件**: `notification/src/main/java/com/hmdm/notification/persistence/domain/PushMessage.java`

```java
// 第50-53行：已定义的远程命令类型
public static final String TYPE_REBOOT = "reboot";
public static final String TYPE_LOCK = "lock";
public static final String TYPE_FACTORY_RESET = "factoryReset";

// 第55-56行：已定义的照片上传类型
public static final String TYPE_UPLOAD_PHOTOS = "uploadPhotos";

// 第58-64行：已定义的远程控制类型
public static final String TYPE_REMOTE_CONTROL_START = "remoteControlStart";
public static final String TYPE_REMOTE_CONTROL_STOP = "remoteControlStop";
public static final String TYPE_REMOTE_CONTROL_WEBRTC_OFFER = "remoteControlWebRTCOffer";
public static final String TYPE_REMOTE_CONTROL_WEBRTC_ANSWER = "remoteControlWebRTCAnswer";
public static final String TYPE_REMOTE_CONTROL_ICE_CANDIDATE = "remoteControlICECandidate";
public static final String TYPE_REMOTE_CONTROL_COMMAND = "remoteControlCommand";

// 第66-69行：已定义的网络过滤类型
public static final String TYPE_UPDATE_NETWORK_RULES = "updateNetworkRules";
public static final String TYPE_UPDATE_NETWORK_SETTINGS = "updateNetworkSettings";
public static final String TYPE_SYNC_NETWORK_CONFIG = "syncNetworkConfig";

// 第71-74行：已定义的联系人同步类型
public static final String TYPE_SYNC_CONTACTS = "syncContacts";
public static final String TYPE_UPDATE_CONTACTS = "updateContacts";
public static final String TYPE_DELETE_CONTACTS = "deleteContacts";
```

### 2.2 PushService 远程命令方法（已存在！）

**文件**: `notification/src/main/java/com/hmdm/notification/PushService.java`

```java
// 第103-112行：发送单个远程命令
@Transactional
public void sendRemoteCommand(Integer deviceId, String commandType) {
    final Device dbDevice = this.deviceDAO.getDeviceById(deviceId);
    if (dbDevice != null) {
        PushMessage message = new PushMessage();
        message.setDeviceId(dbDevice.getId());
        message.setMessageType(commandType);  // "reboot", "lock", "factoryReset"
        this.send(message);
    }
}

// 第120-133行：批量发送远程命令
@Transactional
public void sendRemoteCommandBulk(List<Integer> deviceIds, String commandType) {
    if (deviceIds != null) {
        deviceIds.forEach(deviceId -> {
            final Device dbDevice = this.deviceDAO.getDeviceById(deviceId);
            if (dbDevice != null) {
                PushMessage message = new PushMessage();
                message.setDeviceId(dbDevice.getId());
                message.setMessageType(commandType);
                this.send(message);
            }
        });
    }
}

// 第140-155行：联系人同步（已存在！）
@Transactional
public void syncContacts(Integer deviceId) {
    sendSimpleMessage(deviceId, PushMessage.TYPE_SYNC_CONTACTS);
}

@Transactional
public void syncContactsBulk(List<Integer> deviceIds) {
    if (deviceIds != null) {
        deviceIds.forEach(this::syncContacts);
    }
}
```

### 2.3 设备实体类（已存在 kioskMode 字段！）

**文件**: `common/src/main/java/com/hmdm/persistence/domain/Device.java`（第99行，第324-330行）

```java
// 设备MDM状态字段
private Boolean mdmMode;
private Boolean kioskMode;
private String androidVersion;
private Long enrollTime;
private String serial;

// Getter/Setter
public Boolean getMdmMode() { return mdmMode; }
public void setMdmMode(Boolean mdmMode) { this.mdmMode = mdmMode; }
public Boolean getKioskMode() { return kioskMode; }
public void setKioskMode(Boolean kioskMode) { this.kioskMode = kioskMode; }
```

### 2.4 Configuration 实体（丰富的Kiosk配置！）

**文件**: `common/src/main/java/com/hmdm/persistence/domain/Configuration.java`（第131-172行）

```java
@ApiModelProperty("Flag enabling the kiosk exit button")
private Boolean kioskExit;

@ApiModelProperty("A flag indicating if MDM is operating in kiosk mode")
private boolean kioskMode;

@ApiModelProperty("Flag enabling Home button in kiosk mode")
private Boolean kioskHome;

@ApiModelProperty("Flag enabling Recents button in kiosk mode")
private Boolean kioskRecents;

@ApiModelProperty("Flag enabling notifications in kiosk mode")
private Boolean kioskNotifications;

@ApiModelProperty("Flag enabling system info in kiosk mode")
private Boolean kioskSystemInfo;

@ApiModelProperty("Flag enabling lock screen in kiosk mode")
private Boolean kioskKeyguard;

@ApiModelProperty("Flag locking power button in kiosk mode")
private Boolean kioskLockButtons;

@ApiModelProperty("Flag forcing screen to be on in kiosk mode")
private Boolean kioskScreenOn;
```

### 2.5 白标/重命名功能（已存在！）

**文件**: `common/src/main/java/com/hmdm/persistence/domain/WhiteLabelBuild.java`

```java
@ApiModelProperty("Application name")
private String appName;          // 自定义应用名称

@ApiModelProperty("Package name")
private String packageName;      // 自定义包名

@ApiModelProperty("Icon file path")
private String iconPath;         // 自定义图标

@ApiModelProperty("Splash screen path")
private String splashScreenPath; // 自定义启动图

@ApiModelProperty("Custom strings (JSON)")
private String customStrings;    // 自定义字符串

@ApiModelProperty("Custom colors (JSON)")
private String customColors;     // 自定义颜色
```

**文件**: `server/src/main/java/com/hmdm/rest/resource/SyncResource.java`（第487-492行）

```java
// Rebranding data - 在设备同步时下发
if (!mobileAppName.equals("")) {
    data.setAppName(mobileAppName);
}
if (!vendor.equals("")) {
    data.setVendor(vendor);
}
```

### 2.6 插件架构参考（以 messaging 为例）

**文件**: `plugins/messaging/src/main/java/com/hmdm/plugins/messaging/rest/MessagingResource.java`

```java
@Singleton
@Path("/plugins/messaging")  // REST端点路径
@Api(tags = {"Messaging plugin"})
public class MessagingResource {

    @Inject
    public MessagingResource(MessagingDAO messagingDAO,
                             UnsecureDAO unsecureDAO,
                             DeviceDAO deviceDAO,
                             PushService pushService,  // 注入Push服务
                             PluginStatusCache pluginStatusCache) {
        // ...
    }

    // 发送消息示例（第222-239行）
    private boolean sendSingleMessage(Message message) {
        try {
            this.messagingDAO.insertMessage(message);
            PushMessage pushMessage = new PushMessage();
            pushMessage.setDeviceId(message.getDeviceId());
            pushMessage.setPayload("{id:" + message.getId() + ",text:\"...\"}");
            pushMessage.setMessageType("textMessage");  // 自定义消息类型
            this.pushService.send(pushMessage);  // 发送Push
            return true;
        } catch (Exception e) {
            logger.error("...", e);
            return false;
        }
    }
}
```

---

## 3. 客户端源码集成点

### 3.1 现有Push消息类型（需扩展）

**文件**: `app/src/main/java/com/hmdm/launcher/json/PushMessage.java`

```java
// 现有类型（第31-47行）
public static final String TYPE_CONFIG_UPDATING = "configUpdating";
public static final String TYPE_CONFIG_UPDATED = "configUpdated";
public static final String TYPE_RUN_APP = "runApp";
public static final String TYPE_BROADCAST = "broadcast";
public static final String TYPE_UNINSTALL_APP = "uninstallApp";
public static final String TYPE_DELETE_FILE = "deleteFile";
public static final String TYPE_PURGE_DIR = "purgeDir";
public static final String TYPE_DELETE_DIR = "deleteDir";
public static final String TYPE_PERMISSIVE_MODE = "permissiveMode";
public static final String TYPE_RUN_COMMAND = "runCommand";
public static final String TYPE_REBOOT = "reboot";           // 已存在
public static final String TYPE_EXIT_KIOSK = "exitKiosk";
public static final String TYPE_CLEAR_DOWNLOADS = "clearDownloadHistory";
public static final String TYPE_INTENT = "intent";
public static final String TYPE_GRANT_PERMISSIONS = "grantPermissions";
public static final String TYPE_ADMIN_PANEL = "adminPanel";
public static final String TYPE_CLEAR_APP_DATA = "clearAppData";

// TODO: 需要新增以下类型（与后端保持一致）
// public static final String TYPE_LOCK_DEVICE = "lock";  // 后端是 "lock"
// public static final String TYPE_FACTORY_RESET = "factoryReset";
// public static final String TYPE_UPLOAD_PHOTOS = "uploadPhotos";
// public static final String TYPE_SYNC_CONTACTS = "syncContacts";
// public static final String TYPE_REMOTE_CONTROL_START = "remoteControlStart";
// ... 等等
```

### 3.2 Push消息处理器（需扩展）

**文件**: `app/src/main/java/com/hmdm/launcher/worker/PushNotificationProcessor.java`

```java
// 现有处理逻辑（第60-140行）
public static void process(PushMessage message, Context context) {
    // ...
    if (message.getMessageType().equals(PushMessage.TYPE_REBOOT)) {
        AsyncTask.execute(() -> reboot(context));  // 第102-104行
        return;
    }
    // ...

    // 第133-139行：发送到插件（广播给其他应用）
    Intent intent = new Intent(Const.INTENT_PUSH_NOTIFICATION_PREFIX + message.getMessageType());
    JSONObject jsonObject = message.getPayloadJSON();
    if (jsonObject != null) {
        intent.putExtra(Const.INTENT_PUSH_NOTIFICATION_EXTRA, jsonObject.toString());
    }
    context.sendBroadcast(intent);
}

// 重启实现（第360-369行）
private static void reboot(Context context) {
    RemoteLogger.log(context, Const.LOG_WARN, "Rebooting by a Push message");
    if (Utils.checkAdminMode(context)) {
        if (!Utils.reboot(context)) {
            RemoteLogger.log(context, Const.LOG_WARN, "Reboot failed");
        }
    } else {
        RemoteLogger.log(context, Const.LOG_WARN, "Reboot failed: no permissions");
    }
}

// TODO: 需要新增的处理逻辑
// private static void lockDevice(Context context, JSONObject payload) { ... }
// private static void factoryReset(Context context, JSONObject payload) { ... }
// private static void uploadPhotos(Context context, JSONObject payload) { ... }
```

### 3.3 REST API接口（需扩展）

**文件**: `app/src/main/java/com/hmdm/launcher/server/ServerService.java`

```java
// 现有API（第45-119行）
// 设备注册/同步
@POST("{project}/rest/public/sync/configuration/{number}")
Call<ServerConfigResponse> enrollAndGetServerConfig(...);

// 设备信息上报
@POST("{project}/rest/public/sync/info")
Call<ResponseBody> sendDevice(@Path("project") String project, @Body DeviceInfo deviceInfo);

// Push查询
@GET("{project}/rest/notifications/device/{number}")
Call<PushResponse> queryPushNotifications(...);

// 位置上报
@PUT("{project}/rest/plugins/devicelocations/public/update/{number}")
Call<ResponseBody> sendLocations(...);

// 重置确认
@POST("{project}/rest/plugins/devicereset/public/{number}")
Call<ResponseBody> confirmDeviceReset(...);

@POST("{project}/rest/plugins/devicereset/public/reboot/{number}")
Call<ResponseBody> confirmReboot(...);

@POST("{project}/rest/plugins/devicereset/public/password/{number}")
Call<ResponseBody> confirmPasswordReset(...);

// TODO: 需要新增以下API
// - 文件上传API（照片上传）
// - LDAP认证API
// - 设备配置导出/导入API
```

### 3.4 ServerConfig（已支持白标字段！）

**文件**: `app/src/main/java/com/hmdm/launcher/json/ServerConfig.java`（第109-110行）

```java
private String appName;   // 从服务器下发的自定义应用名称
private String vendor;    // 从服务器下发的供应商名称
```

### 3.5 ProUtils模式（已确认）

**文件**: `app/src/main/java/com/hmdm/launcher/pro/ProUtils.java`

```java
// 基础版本返回false/空实现（第39-134行）
public class ProUtils {
    public static boolean isPro() {
        return false;  // Enterprise版本返回true
    }

    public static boolean startCosuKioskMode(String kioskApp, Activity activity, boolean enableSettings) {
        return false;  // Enterprise版本实现COSU逻辑
    }

    public static void processLocation(Context context, Location location, String provider) {
        // 基础版本：空实现
        // Enterprise版本：实现位置处理和批量上传
    }

    public static String getAppName(Context context) {
        return context.getString(R.string.app_name);
    }

    public static String getCopyright(Context context) {
        return "(c) " + Calendar.getInstance().get(Calendar.YEAR) + " " + context.getString(R.string.vendor);
    }
}
```

### 3.6 位置服务（已存在，需ProUtils扩展）

**文件**: `app/src/main/java/com/hmdm/launcher/service/LocationService.java`

```java
// 位置监听（第69-99行）
private LocationListener gpsLocationListener = new LocationListener() {
    @Override
    public void onLocationChanged(Location location) {
        RemoteLogger.log(LocationService.this, Const.LOG_VERBOSE,
            "GPS location update: lat=" + location.getLatitude() + ", lon=" + location.getLongitude());
        ProUtils.processLocation(LocationService.this, location, LocationManager.GPS_PROVIDER);
        // 调用ProUtils，Enterprise版本可实现批量上传
    }
    // ...
};

// 位置更新间隔（第65行）
private static final int LOCATION_UPDATE_INTERVAL = 60000;  // 60秒
```

---

## 4. 前后端集成矩阵

### 4.1 功能实现状态对照

| 功能 | 后端PushMessage常量 | PushService方法 | 客户端PushMessage | 处理器实现 | 状态 |
|------|---------------------|-----------------|-------------------|------------|------|
| 重启 | `TYPE_REBOOT` (已定义) | `sendRemoteCommand` (已存在) | `TYPE_REBOOT` (已存在) | `reboot()` (已存在) | **已完成** |
| 锁定 | `TYPE_LOCK` (已定义) | `sendRemoteCommand` (已存在) | **需添加** | **需实现** | 待开发 |
| 恢复出厂 | `TYPE_FACTORY_RESET` (已定义) | `sendRemoteCommand` (已存在) | **需添加** | **需实现** | 待开发 |
| 照片上传 | `TYPE_UPLOAD_PHOTOS` (已定义) | 需添加 | **需添加** | **需实现** | 待开发 |
| 联系人同步 | `TYPE_SYNC_CONTACTS` (已定义) | `syncContacts` (已存在) | **需添加** | **需实现** | 待开发 |
| 远程控制 | `TYPE_REMOTE_CONTROL_*` (已定义) | 需添加 | **需添加** | **需实现** | 待开发 |
| 网络过滤 | `TYPE_UPDATE_NETWORK_RULES` (已定义) | 需添加 | **需添加** | **需实现** | 待开发 |
| Kiosk模式 | Configuration.kioskMode (已存在) | N/A | `TYPE_EXIT_KIOSK` (已存在) | `ProUtils.startCosuKioskMode` (需实现) | 部分完成 |
| 位置追踪 | N/A | N/A | N/A | `ProUtils.processLocation` (需实现) | 需Enterprise实现 |
| LDAP集成 | **需添加** | **需添加** | **需添加** | **需实现** | 待开发 |
| 设备导出/导入 | **需添加** | **需添加** | **需添加** | **需实现** | 待开发 |
| 白标软件 | `Configuration.appName` (已存在) | N/A | N/A | `ProUtils.getAppName` (需实现) | 部分完成 |

### 4.2 REST API端点对照

| 功能 | 后端端点 | 客户端ServerService方法 | 状态 |
|------|----------|-------------------------|------|
| 设备注册 | `/rest/public/sync/configuration/{number}` | `enrollAndGetServerConfig` | 已存在 |
| 配置同步 | `/rest/public/sync/configuration/{number}` | `getServerConfig` | 已存在 |
| 设备信息上报 | `/rest/public/sync/info` | `sendDevice` | 已存在 |
| Push查询 | `/rest/notifications/device/{number}` | `queryPushNotifications` | 已存在 |
| 位置上报 | `/rest/plugins/devicelocations/public/update/{number}` | `sendLocations` | 已存在 |
| 设备重置确认 | `/rest/plugins/devicereset/public/{number}` | `confirmDeviceReset` | 已存在 |
| 重启确认 | `/rest/plugins/devicereset/public/reboot/{number}` | `confirmReboot` | 已存在 |
| 文件上传 | **需添加: `/rest/plugins/fileupload/public/upload/{number}`** | **需添加** | 待开发 |
| LDAP认证 | **需添加: `/rest/plugins/ldap/public/auth/{number}`** | **需添加** | 待开发 |
| 设备配置导出 | **需添加: `/rest/plugins/deviceexport/public/export/{number}`** | **需添加** | 待开发 |

---

## 5. 插件开发指南（后端）

### 5.1 创建新插件步骤

以"照片上传插件"为例：

```
plugins/photoupload/
├── pom.xml
└── src/main/java/com/hmdm/plugins/photoupload/
    ├── PhotoUploadPluginConfigurationImpl.java  # 插件配置实现
    ├── guice/module/
    │   ├── PhotoUploadLiquibaseModule.java      # 数据库迁移
    │   ├── PhotoUploadPersistenceModule.java    # DAO绑定
    │   └── PhotoUploadRestModule.java           # REST资源绑定
    ├── persistence/
    │   ├── PhotoUploadDAO.java
    │   ├── mapper/PhotoUploadMapper.java
    │   └── domain/UploadedPhoto.java
    └── rest/
        ├── PhotoUploadResource.java             # REST API
        └── json/PhotoUploadFilter.java
```

### 5.2 插件关键代码模板

**PhotoUploadResource.java**:

```java
@Singleton
@Path("/plugins/photoupload")
@Api(tags = {"Photo Upload plugin"})
public class PhotoUploadResource {

    @Inject
    public PhotoUploadResource(PhotoUploadDAO photoUploadDAO,
                               DeviceDAO deviceDAO,
                               PushService pushService) {
        // ...
    }

    // 触发设备上传照片
    @POST
    @Path("/private/requestUpload/{deviceId}")
    public Response requestPhotoUpload(@PathParam("deviceId") Integer deviceId,
                                       @QueryParam("path") String path) {
        // 发送Push消息
        PushMessage message = new PushMessage();
        message.setDeviceId(deviceId);
        message.setMessageType(PushMessage.TYPE_UPLOAD_PHOTOS);
        message.setPayload("{path:\"" + path + "\"}");
        pushService.send(message);
        return Response.OK();
    }

    // 接收设备上传的照片
    @POST
    @Path("/public/upload/{number}")
    @Consumes(MediaType.MULTIPART_FORM_DATA)
    public Response uploadPhoto(@PathParam("number") String deviceNumber,
                                @FormDataParam("file") InputStream fileInputStream,
                                @FormDataParam("metadata") String metadata) {
        // 保存上传的文件
        // ...
        return Response.OK();
    }
}
```

---

## 6. 实现优先级（基于代码复杂度）

| 优先级 | 功能 | 后端改动 | 客户端改动 | 说明 |
|--------|------|----------|------------|------|
| P0 | 远程锁定/恢复出厂 | 小（已存在PushService.sendRemoteCommand） | 小（添加处理器） | 后端已准备好，只需客户端处理 |
| P0 | 联系人同步 | 小（已存在PushService.syncContacts） | 中（需要ContactsContract API） | 后端已准备好 |
| P1 | 照片上传 | 中（需新建插件） | 中（需文件上传逻辑） | 需要新建后端插件 |
| P1 | Kiosk模式增强 | 小 | 中（需COSU实现） | ProUtils.startCosuKioskMode需要实现 |
| P1 | 白标软件 | 小（字段已存在） | 小（ProUtils方法实现） | Configuration.appName字段已存在 |
| P1 | 位置追踪增强 | 小 | 小（ProUtils.processLocation实现） | 主要是Enterprise版本实现 |
| P2 | LDAP集成 | 大（需新建插件+LDAP库） | 中（需认证UI） | 需要引入LDAP库 |
| P2 | 设备导出/导入 | 中（需新建插件） | 中（需配置序列化） | 需要新建后端插件 |
| P3 | 远程控制 | 大（需WebRTC） | 大（需WebRTC+录屏） | 复杂度高 |
| P3 | 网络流量过滤 | 大（需VPN服务） | 大（需VPN服务实现） | 复杂度高 |

---

## 7. 关键代码复用点

### 7.1 后端可复用组件

1. **PushService.send()** - 已支持MQTT和Polling双通道
2. **PushService.sendRemoteCommand()** - 远程命令发送
3. **PushService.sendRemoteCommandBulk()** - 批量命令发送
4. **PushService.syncContacts()** - 联系人同步命令
5. **Plugin架构** - messaging/deviceinfo插件可直接参考

### 7.2 客户端可复用组件

1. **PushNotificationProcessor.process()** - 消息分发框架
2. **Utils.isDeviceOwner()** - DeviceOwner权限检查
3. **Utils.reboot()** - 重启实现参考
4. **RemoteLogger.log()** - 远程日志上报
5. **ServerService** - Retrofit API框架

### 7.3 Pro/Enterprise切换机制

```java
// 代码中通过ProUtils.isPro()判断
if (ProUtils.isPro()) {
    // Enterprise功能
    ProUtils.processLocation(context, location, provider);
} else {
    // 基础版本：空操作或简化处理
}

// 或者通过方法返回值判断
if (!ProUtils.startCosuKioskMode(kioskApp, activity, enableSettings)) {
    // 如果返回false（基础版本），显示提示
    Toast.makeText(context, "Kiosk mode requires Enterprise version", Toast.LENGTH_SHORT).show();
}
```

---

## 8. 总结

### 8.1 后端现状

**后端已完成的准备工作**：
1. PushMessage类型已定义大部分Enterprise功能常量
2. PushService已实现远程命令发送框架
3. Device实体已包含mdmMode/kioskMode字段
4. Configuration实体包含丰富的kiosk配置选项
5. WhiteLabelBuild/RebrandingSettings已支持白标功能
6. SyncResource已下发appName/vendor到客户端
7. 插件架构成熟，已有多个插件示例

**后端需要补充的工作**：
1. 照片上传、LDAP、设备导出导入等新插件
2. 部分Push消息类型的处理方法

### 8.2 客户端现状

**客户端已完成的准备工作**：
1. Push消息处理框架已就绪
2. 重启(TYPE_REBOOT)功能已实现
3. ProUtils模式清晰，便于Enterprise扩展
4. LocationService已集成ProUtils.processLocation调用点
5. ServerConfig已包含appName/vendor字段接收白标配置

**客户端需要补充的工作**：
1. PushMessage.java中补充缺失的常量定义（TYPE_LOCK, TYPE_FACTORY_RESET等）
2. PushNotificationProcessor中添加新消息类型的处理
3. ServerService中补充新的REST API接口
4. ProUtils中实现Enterprise版本的实际功能

### 8.3 推荐的集成顺序

1. **第一阶段**：远程锁定/恢复出厂（后端已就绪，只需客户端处理）
2. **第二阶段**：联系人同步（后端已就绪）
3. **第三阶段**：照片上传（需要新建后端插件）
4. **第四阶段**：Kiosk模式增强（主要是COSU实现）
5. **第五阶段**：白标软件（简单，字段已存在）
6. **第六阶段**：LDAP、设备导出导入、远程控制、网络过滤
